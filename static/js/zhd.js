// Generated by CoffeeScript 1.6.2
(function() {
  var HomeCtrl, PlaylistCtrl;

  angular.module("zhd", []).config(function($routeProvider) {
    return $routeProvider.when("/", {
      controller: HomeCtrl,
      templateUrl: "/static/views/home.html"
    }).when("/:id", {
      controller: PlaylistCtrl,
      templateUrl: "/static/views/playlist.html"
    }).otherwise({
      redirectTo: "/"
    });
  });

  HomeCtrl = function($scope) {
    return $scope.letsRock = function() {
      return alert("OPPA GANGNAM STYLE!!!");
    };
  };

  PlaylistCtrl = function($scope, $routeParams, $http) {
    var playlistId;

    playlistId = $routeParams.id;
    $scope.playlistName = playlistId;
    $http.get("/static/tracks.json").success(function(data) {
      return $scope.tracks = data;
    }).error(function() {
      return alert('Error fetching the playlist :( Try to reload the page.');
    });
    $scope.playTrack = function(id) {
      return $scope.dz.player.playTracks([id]);
    };
    return $scope.buildPlayer = function() {
      DZ.init({
        appId: '116267',
        channelUrl: 'http://developers.deezer.com/examples/channel.php',
        player: {
          container: 'player',
          onload: function() {}
        }
      });
      return $scope.dz = DZ;
    };
  };

}).call(this);
